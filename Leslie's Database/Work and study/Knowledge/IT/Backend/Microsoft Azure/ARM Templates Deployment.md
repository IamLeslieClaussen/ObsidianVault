A way to define resources like VMs or databases using code in form of JSON text.
It can define multiple resources, some of which may depend on others to form a complete deployment.
DB cannot work without a database server. 
The importance of Templates, is that it creates consistency and speed.